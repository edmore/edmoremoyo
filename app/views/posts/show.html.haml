=render :partial => 'home/site_header'

%section{:id => "post"}
  %article{:class => "posts", :id => "post_#{@post.id}"}
    %header
      %h2= @post.title
      =link_to "Back to Index" , blog_path
    = raw(@post.body_to_html)
    %footer= "{:posted => #{@post.date_to_readable}}"

= raw(disqus_thread)


=render :partial => 'home/site_footer'
